{
  "manifest_version": "0.3",
  "name": "namecheap-pp-mcp",
  "display_name": "Namecheap",
  "version": "4.6.0",
  "description": "Manage Namecheap domains, DNS, account balances, pricing, contacts, TLDs, and SSL metadata from the terminal using Namecheap’s XML API.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/namecheap-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/namecheap-pp-mcp",
      "args": [],
      "env": {
        "NAMECHEAP_API_KEY_QUERY": "${user_config.namecheap_api_key_query}"
      }
    }
  },
  "user_config": {
    "namecheap_api_key_query": {
      "type": "string",
      "title": "NAMECHEAP_API_KEY_QUERY",
      "description": "Sets NAMECHEAP_API_KEY_QUERY for the Namecheap MCP server.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
